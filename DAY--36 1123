#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;
    
    while (T--) {
        long long L, K;
        cin >> L >> K;

        long long base = L / K;
        long long remainder = L % K;
       
        cout << remainder << endl;
    }

    return 0;
}
