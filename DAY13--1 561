#include <iostream>
using namespace std;

int main() {
    int a; 
    cin >> a;

    while (a--) {
        int X, Y; 
        cin >> X >> Y;

        if (Y >= (X + 1) / 2) {
            cout << "YES" << endl;
        } else {
            cout << "NO" << endl;
        }
    }

    return 0;
}
